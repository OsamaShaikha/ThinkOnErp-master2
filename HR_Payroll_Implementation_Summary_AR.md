# التقرير الشامل لتنفيذ وتطوير نظام الموارد البشرية والرواتب (HR & Dynamic Payroll Engine)
**ThinkOnErp - Dynamic HR & Payroll Module Implementation**

---

## 1. الرؤية المعمارية والمبادئ الأساسية (Core Architectural Principles)

تم بناء وتطوير نظام الموارد البشرية والرواتب ليعمل بشكل **ديناميكي 100%** معتمداً على أفضل الممارسات في الـ Clean Architecture والأنظمة العالمية (Enterprise ERP):

1. **انعدام القيم الثابتة بالكود (Zero Hardcoding)**:
   - لا توجد أي نسب اقتطاع، شرائح ضريبية، سقوف رواتب خاضعة للضمان، مضاعفات عمل إضافي، فترات سماح للبصمات، أيام عطل أسبوعية أو رسمية، أو معاملات تناسب راتب (Proration Divisors) مكتوبة كقيم ثابتة داخل الكود البرمجي (C#). كل شيء يتم جلبه وتطبيقه ديناميكياً من قواعد البيانات.
2. **السياسات على مستوى الشركة والصلاحية الزمنية (Company-Level & Effective Dating)**:
   - جميع السياسات والتقاويم مرتبطة برقم الشركة (`CompanyId`) وتحتوي على فترات سريان زمنية (`EffectiveFrom` و `EffectiveTo`)، مما يتيح تغيير السياسات واللوائح مستقبلاً دون التأثير على المسيرات التاريخية السابقة.
3. **السقف التلقائي للاقتطاعات والترحيل الآلي للمتبقي (Automatic Deduction Capping & Carry-Forward)**:
   - في حال تجاوز مجموع أقساط القروض والسلف سقف الاقتطاع المسموح به من صافي الراتب (مثل 33% أو النسبة المحددة بالسياسة)، يقوم النظام آلياً باقتطاع الحد الأقصى فقط، وترحيل المبلغ المتبقي (Carry-Forward) إلى الفترات اللاحقة دون إيقاف أو فشل احتساب مسير الرواتب.
4. **تعدد خيارات احتساب التناسب (Dynamic Proration Methods)**:
   - تحديد آلية احتساب راتب الموظف المنضم أو المنتهي خدماته في منتصف الشهر وفق السياسة المعتمدة في الشركة:
     - **الأيام الفعلية للشهر (`CALENDAR_DAYS`)**: القسمة على أيام الشهر الحقيقي (28، 29، 30، 31).
     - **الأساس التجاري الثابت (`FIXED_30`)**: القسمة على 30 يوماً ثابتاً لجميع الشهور.
     - **أيام العمل الفعلية (`WORKING_DAYS`)**: القسمة على أيام العمل المحسوبة من تقويم الشركة واستثناء العطل.
5. **الترحيل المالي المزدوج المتوازن (Balanced Double-Entry GL Posting)**:
   - توليد سندات قيد محاسبية متوازنة (General Ledger Journal Vouchers) آلياً عند ترحيل المسير، وإمكانية إلغاء المسير وتوليد قيد عكسي متوازن (Reversal Voucher).
6. **التوثيق واللقطات الحسابية (Payroll Calculation Snapshot & Auditing)**:
   - تخزين لقطة تفصيلية (`Snapshot`) لجميع المدخلات والقوانين والسياسات التي تم تطبيقها لحظة احتساب راتب كل موظف، مع خدمة شرح تفكيكي لصافي الراتب (`Payroll Explanation`).

---

## 2. كيانات النطاق الجديدة (Domain Entities)

تمت إضافة الكيانات التالية داخل مسار `src/ThinkOnErp.Domain/Entities/Hr/`:

| الكيان (Entity) | الوصف والدور في النظام |
| :--- | :--- |
| **`WorkCalendar`** | تقويم الشركة لتحديد أيام العمل الرسمية، العطل الأسبوعية، وفترات العمل وساعات الدوام اليومية. |
| **`WorkCalendarDay`** | تفاصيل الأيام السنوية للتقويم (تاريخ اليوم، اسم اليوم، نوع اليوم: عمل / عطلة أسبوعية / عطلة رسمية). |
| **`PublicHoliday`** | جدول العطل والإجازات الرسمية للشركة مع تحديد ما إذا كانت مدفوعة أم لا، وهل هي سنوية متكررة. |
| **`AttendancePolicy`** | سياسة الحضور والانصراف: فترات السماح للتأخير (`Grace Period`)، قواعد تقريب أوقات البصمة، وغرامات نسيان البصمة. |
| **`RawAttendance`** | سجل حركات البصمات الخام المستوردة من أجهزة البصمة البيومترية مع تشفير منع التكرار (`PunchHash`). |
| **`AttendanceDay`** | اليوم المحتسب للحضور: ساعات العمل المجدولة، ساعات العمل الفعلية، دقائق التأخير، الخروج المبكر، وساعات الإضافي. |
| **`AttendanceCorrectionRequest`** | دورة طلبات تصحيح حركات البصمة المفقودة (`Missing Punch`) والاعتماد من المسؤولين. |
| **`OvertimeRule`** | قواعد ومعاملات احتساب العمل الإضافي (الأيام العادية، عطلة نهاية الأسبوع، والأعياد الرسمية). |
| **`ProrationPolicy`** | سياسة التناسب للرواتب الجزئية (`CALENDAR_DAYS`, `FIXED_30`, `WORKING_DAYS`). |
| **`DeductionPolicy`** | سياسات سقوف الاقتطاع من صافي الراتب، نسبة الحد الأقصى، وتفعيل الترحيل الآلي للمتبقي. |
| **`TaxPolicy`** | السياسة الضريبية العامة: سقف الإعفاء الشخصي، إعفاء المعالين، وسقف المساهمة الوطنية. |
| **`TaxBracket`** | الشرائح التصاعدية لضريبة الدخل (الحد الأدنى، الحد الأعلى، والنسبة المئوية لكل شريحة). |
| **`SSCPolicy`** | سياسة الضمان الاجتماعي: نسبة اقتطاع الموظف، نسبة الشركة، سقف الراتب الخاضع، ورسوم المهن الخطرة. |
| **`EmployeeLoan`** | القروض طويلة ومتوسطة الأجل للموظفين مع تفاصيل جدول السداد وحالة القرض. |
| **`LoanRepaymentSchedule`** | جدول الأقساط الشهرية للقرض: المبلغ المجدول، المبلغ المسدد، المبلغ المرحل، وحالة القسط. |
| **`EmployeeAdvance`** | السلف النقدية المؤقتة وتحديد شهر الاسترداد المستهدف. |
| **`PayrollPeriod`** | تعريف الفترات المالية الشهرية للرواتب (حالة الفترة: مفتوحة، مغلقة، تحت المعالجة). |
| **`PayrollCalculationSnapshot`** | لقطة الأرشفة والتدقيق لكل سطر راتب موظف يوثق بالتفصيل جميع المتغيرات والسياسات المستخدمة. |

---

## 3. طبقة البنية التحتية وقواعد بيانات أوراكل (Infrastructure & Oracle EF Core)

### 1. تكوينات الكيانات (Configurations):
تم إنشاء 18 ملف تكوين Fluent API داخل مسار `src/ThinkOnErp.Infrastructure/Data/Configurations/Hr/` مهيأة بالكامل لبيئة **Oracle Database**:
- ضبط الـ Sequences ومولدات المفاتيح الأساسية (`Identity / Sequence`).
- ضبط دقة الحقول المالية `(18, 3)` والنسب المئوية `(5, 4)`.
- ربط العلاقات الأجنبية (Foreign Keys) مع ضوابط الحذف (`Restrict` / `Cascade`).
- بناء الفهارس (Indexes) والقيود الفريدة لمنع التكرار (Unique Constraints على الكود والسنة والشهر).

### 2. المستودعات (Repositories):
تم إنشاء واجهات وتطبيقات المستودعات التالية وتسجيلها في الـ Dependency Injection:
- `WorkCalendarRepository` & `IWorkCalendarRepository`: للتقاويم والعطل.
- `AttendanceCorrectionRepository` & `IAttendanceCorrectionRepository`: لسجلات البصمات وطلبات التصحيح.
- `PolicyRepository` & `IPolicyRepository`: لجلب السياسات الفعالة حسب تاريخ الفترة والشركة.
- `LoanRepository` & `ILoanRepository`: لإدارة القروض، جداول الأقساط، والسلف.
- `PayrollPeriodRepository` & `IPayrollPeriodRepository`: لإدارة الفترات الشهرية والـ Snapshots.

---

## 4. المحركات والخدمات البرمجية (Application Engines & Services)

تم تطوير كافة المحركات الذكية داخل `src/ThinkOnErp.Application/Services/Hr/`:

### 1. `WorkCalendarService` (خدمة تقويم العمل والعطل):
- توليد أيام السنة بالكامل آلياً استناداً لأيام العمل وعطلات نهاية الأسبوع المحددة للشركة.
- فحص ما إذا كان يوم محدد يوم عمل أو عطلة رسمية أو نهاية أسبوع.
- احتساب عدد أيام العمل الفعلية بين أي تاريخين لخدمة محرك التناسب.

### 2. `AttendanceCalculationEngine` (محرك معالجة الحضور والدوام):
- استقبال الحركات الخام مع توليد `PunchHash` (SHA256) لمنع تكرار الحركة عند سحبها أكثر من مرة.
- مطابقة البصمات مع جدول وردية الموظف (`ShiftSchedule`).
- معالجة الورديات الليلية المتقاطعة مع منتصف الليل (`Overnight Shifts`).
- احتساب دقائق التأخير الصباحي والخروج المبكر بعد استبعاد فترة السماح المحددة بالسياسة (`Grace Period`).
- احتساب ساعات العمل الفعلي وساعات العمل الإضافي المعتمدة.

### 3. `AttendanceCorrectionService` (خدمة تصحيح البصمات المفقودة):
- إتاحة تقديم طلبات تصحيح نسيان البصمة من قبل الموظف أو المشرف.
- دورة الاعتماد والموافقة أو الرفض، مع التحديث الآلي الفوري لسجل الحضور اليومي `AttendanceDay`.

### 4. `OvertimeCalculationService` (محرك العمل الإضافي الديناميكي):
- جلب قواعد الإضافي للشركة حسب تاريخ الاحتساب.
- اشتقاق أجر الساعة للموظف بناءً على الراتب الأساسي وساعات العمل الشهرية المعتمدة.
- احتساب قيمة الاستحقاق المالي للإضافي حسب نوع اليوم (يوم عمل عادي x1.25، عطلة أسبوعية x1.50، عطلة رسمية x2.00 أو حسب السياسة).

### 5. `PayrollProrationService` (محرك التناسب للرواتب الجزئية):
- معالجة تعيين الموظفين في منتصف الشهر أو إنهاء الخدمات قبل نهاية الشهر.
- جلب سياسة التناسب الفعالة للشركة واحتساب المعامل بدقة (`CALENDAR_DAYS`, `FIXED_30`, `WORKING_DAYS`).

### 6. `SSCCalculationService` (محرك الضمان الاجتماعي):
- جلب نسب الاقتطاع من السياسة الفعالة للشركة وتطبيق نسب الموظف والشركة.
- تطبيق السقف الأعلى للأجر الخاضع للضمان (Capping at Max Salary Ceiling).
- إضافة علاوة المهن الخطرة (+1%) على حصة المنشأة في حال كان تصنيف الموظف مهنة خطرة.

### 7. `TaxCalculationEngine` (محرك ضريبة الدخل والشرائح التصاعدية):
- احتساب الإعفاءات الشخصية والعائلية حسب عدد المعالين.
- تطبيق الشرائح التصاعدية المتعددة لضريبة الدخل (مثل 5%، 10%، 15%، 20%، 25%).
- احتساب المساهمة الوطنية للتكافل (1%) للمداخيل الخاضعة التي تتجاوز السقف القانوني.

### 8. `LoanDeductionService` (محرك القروض والسلف والسقف الآلي):
- توليد جداول الأقساط الشهرية تلقائياً عند اعتماد القرض.
- تتبع السلف المستحقة في شهر الرواتب.
- **تطبيق سقف الاقتطاع والترحيل الآلي**: فحص صافي الراتب القابل للاقتطاع، وفي حال تجاوز القسط السقف، يتم اقتطاع المسموح به وترحيل المبلغ المتبقي (`CarriedForwardAmount`) للدفعة القادمة وتعديل حالة القسط إلى `PARTIAL`.

### 9. `PayrollCalculationEngine` (المحرك الرئيسي لاحتساب مسير الرواتب):
- إدارة خط المعالجة المتكامل للراتب خطوة بخطوة:
  1. جلب الهيكل الراتبي والبدلات الثابتة.
  2. تطبيق معامل التناسب (`Proration`).
  3. إضافة استحقاق العمل الإضافي المعتمد.
  4. خصم أيام الإجازات بدون راتب والجزاءات.
  5. احتساب الراتب الإجمالي الخاضع.
  6. احتساب خصم الضمان الاجتماعي وحصة الشركة.
  7. احتساب الاستقطاع الضريبي والمساهمة الوطنية.
  8. احتساب أقساط القروض والسلف مع السقف الآلي.
  9. احتساب صافي الراتب النهائي المستحق وتوليد لقطة الأرشفة والتدقيق `PayrollCalculationSnapshot`.

### 10. `PayrollValidationService` (خدمة الفحص والتحقق الاستباقي):
- التحقق المسبق قبل الحساب وقبل الاعتماد:
  - التأكد من وجود هياكل رواتب فعالة لكافة الموظفين.
  - اكتشاف أي صافي راتب سالب ومعالجته.
  - التحقق من وجود بصمات معلقة أو طلبات تصحيح غير معتمدة.
  - التحقق من توافر السياسات الضريبية والضمان الفعالة.

### 11. `PayrollExplanationService` (خدمة شرح وتفكيك الراتب):
- تزويد الإدارة والموظف بتقرير تحليلي كامل يشرح كيف تم التوصل لصافي الراتب بالتفصيل (أيام العمل، معامل التناسب، البدلات، الشرائح الضريبية المطبقة، ونسب الضمان).

### 12. `PayrollService` (إدارة دورة حياة المسير والربط المالي):
- التحكم بحالات المسير: `DRAFT` ➔ `CALCULATED` ➔ `APPROVED` ➔ `POSTED_TO_GL` ➔ `PAID` / `CANCELLED`.
- **الترحيل للحسابات العامة (GL Posting)**: توليد سند قيد يومية آلي مزدوج ومتوازن:
  - *المدين (Debit)*: حسابات مصاريف الرواتب والأجور ومصاريف مساهمة الشركة بالضمان.
  - *الدائن (Credit)*: أمانات ضريبة الدخل، أمانات الضمان الاجتماعي، ذمم القروض والسلف، ومستحقات الرواتب الصافية.
- **إلغاء المسير والترحيل العكسي**: إمكانية إلغاء المسير المعتمد وتوليد سند قيد عكسي تلقائياً.

---

## 5. واجهات برمجة التطبيقات (REST API Endpoints)

تم إنشاء الـ Controllers التالية داخل مسار `src/ThinkOnErp.API/Controllers/Hr/`:

### 1. التقاويم والعطل (`WorkCalendarsController`):
- `GET /api/v1/hr/work-calendars` : استعراض تقاويم العمل.
- `POST /api/v1/hr/work-calendars` : إنشاء تقويم جديد.
- `POST /api/v1/hr/work-calendars/{id}/generate-days` : توليد أيام السنة للتقويم.
- `GET /api/v1/hr/work-calendars/holidays` : استعراض العطل الرسمية.
- `POST /api/v1/hr/work-calendars/holidays` : إضافة عطلة رسمية.

### 2. تصحيحات البصمة والدوام (`AttendanceCorrectionsController`):
- `GET /api/v1/hr/attendance-corrections` : استعراض طلبات التصحيح.
- `POST /api/v1/hr/attendance-corrections` : تقديم طلب تصحيح بصمة.
- `POST /api/v1/hr/attendance-corrections/{id}/approve` : اعتماد طلب التصحيح.
- `POST /api/v1/hr/attendance-corrections/{id}/reject` : رفض طلب التصحيح.
- `GET /api/v1/hr/attendance-corrections/daily-summary` : تقرير الحضور والدوام اليومي.

### 3. السياسات الديناميكية (`PayrollPoliciesController`):
- إدارة سياسات الحضور `attendance-policy`.
- إدارة قواعد الإضافي `overtime-rules`.
- إدارة سياسات التناسب `proration-policy`.
- إدارة سياسات سقوف الاقتطاع `deduction-policy`.
- إدارة سياسات ضريبة الدخل والشرائح `tax-policy`.
- إدارة سياسات الضمان الاجتماعي `ssc-policy`.

### 4. القروض والسلف (`LoansController`):
- `GET /api/v1/hr/loans` : استعراض قروض الموظفين وأقساطها.
- `POST /api/v1/hr/loans` : طلب قرض جديد وجدولة الأقساط.
- `POST /api/v1/hr/loans/{id}/approve` : اعتماد القرض وتفعيل الجدولة.
- `GET /api/v1/hr/loans/advances` : استعراض السلف النقدية.
- `POST /api/v1/hr/loans/advances` : طلب سلفة مؤقتة.
- `POST /api/v1/hr/loans/advances/{id}/approve` : اعتماد السلفة.

### 5. مسيرات الرواتب (`PayrollController`):
- `POST /api/v1/hr/payroll/runs` : إنشاء مسير جديد.
- `POST /api/v1/hr/payroll/runs/{id}/calculate` : تشغيل الاحتساب الديناميكي الشامل لجميع الموظفين.
- `POST /api/v1/hr/payroll/runs/{id}/approve` : اعتماد مسير الرواتب.
- `POST /api/v1/hr/payroll/runs/{id}/post-gl` : الترحيل المالي وتوليد سند القيد في الحسابات العامة.
- `POST /api/v1/hr/payroll/runs/{id}/cancel` : إلغاء المسير وعكس القيود المالية.
- `GET /api/v1/hr/payroll/runs/{id}/validate` : التحقق والفحص الاستباقي للمسير.
- `GET /api/v1/hr/payroll/explanation` : طلب الشرح التحليلي لراتب الموظف.
- `GET /api/v1/hr/payroll/payslips` : استعراض قسائم الرواتب (Payslips).

---

## 6. التحقق والاختبارات الآلية (Verification & Automated Unit Tests)

### 1. فحص البناء (Build Status):
- تم تنفيذ أمر البناء `dotnet build src/ThinkOnErp.API/ThinkOnErp.API.csproj` مع نجاح كامل **(0 Errors)**.

### 2. نتائج اختبارات الوحدة (Unit Tests):
تم تنفيذ كافة الاختبارات الآلية بنجاح داخل مشروع `tests/ThinkOnErp.HR.Tests`:
- **عدد الاختبارات الكلي**: 27 اختباراً.
- **الاختبارات الناجحة**: 27 اختباراً بنسبة **100%**.
- **الاختبارات الفاشلة**: 0.

```text
Passed!  - Failed: 0, Passed: 27, Skipped: 0, Total: 27, Duration: 250 ms - ThinkOnErp.HR.Tests.dll (net8.0)
```

---

## 7. الخلاصة والجاهزية (Summary)

تم بناء نظام الموارد البشرية ومسير الرواتب ليكون **نظاماً سحابياً متعدد الشركات (Multi-Tenant Enterprise Ready)**، يمتثل لأعلى معايير الحوكمة المالية، وخالٍ تماماً من أي قيود برمجية ثابتة، ومستعد للعمل الفعلي والتكامل مع واجهات المستخدم (Frontend).

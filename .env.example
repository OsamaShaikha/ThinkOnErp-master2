# ThinkOnErp API Environment Variables
# Copy this file to .env and update with your values

# ============================================
# REQUIRED: Oracle Database Connection
# ============================================
# Update with your Oracle database details
ORACLE_CONNECTION_STRING=Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=XEPDB1)));User Id=THINKON_ERP;Password=THINKON_ERP;

# Example for local Oracle:
# ORACLE_CONNECTION_STRING=Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=192.168.1.100)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=ORCL)));User Id=SYSTEM;Password=YourPassword;

# Example for Oracle Cloud:
# ORACLE_CONNECTION_STRING=Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCPS)(HOST=your-db.oraclecloud.com)(PORT=1522))(CONNECT_DATA=(SERVICE_NAME=your_service)));User Id=ADMIN;Password=YourPassword;

# ============================================
# REQUIRED: JWT Settings
# ============================================
# Generate a secure random key (at least 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET_KEY=A7fK9xP2LmQ8vR4TzW6nB1CjD5eH3YUs
JWT_ISSUER=ThinkOnErpAPI
JWT_AUDIENCE=ThinkOnErpClient
JWT_EXPIRY_MINUTES=60

# ============================================
# Optional: Application Settings
# ============================================
# Logging level: Debug, Information, Warning, Error
LOG_LEVEL=Information

# API Port (default: 5000)
API_PORT=5000
